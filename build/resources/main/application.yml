spring:
  application:
    name: Company Search Application
    description: Returns company and officers details

server:
  servlet:
    context-path: /companysearch/rest/api

app:
  company-search:
    remote-client:
      base_url: https://exercise.trunarrative.cloud
      path: /TruProxyAPI/rest/Companies/v1


logging:
  level:
    reactor:
      netty:
        http:
          client: debug
